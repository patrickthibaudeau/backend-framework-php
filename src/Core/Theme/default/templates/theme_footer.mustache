</main>
    </div>{{! closes flex layout wrapper that contains drawer + main }}
    <footer class="mt-8 border-t bg-white">
        <div class="w-full px-4 sm:px-6 lg:px-8 py-6 text-sm text-slate-500 flex flex-col sm:flex-row gap-4 sm:items-center justify-between">
            <div>&copy; {{current_year}} {{site_name}}. {{#str}}all_rights_reserved, theme_default{{/str}}</div>
            <div class="flex gap-4">
                {{#footer_links}}
                <a href="{{url}}" class="hover:text-brand-600 transition-colors">{{label}}</a>
                {{/footer_links}}
            </div>
        </div>
    </footer>

    {{#extra_footer}} {{{extra_footer}}} {{/extra_footer}}

    <!-- Alpine.js (optional lightweight interactivity) -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</body>
</html>
